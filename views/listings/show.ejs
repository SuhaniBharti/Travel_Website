<!-- <% layout("/layouts/boilerplate") %>
  <div class="row mt-3">
    <div class="col-8 offset-2">
  <h3>Listing Details</h3>
  </div>
  <div class="card col-6 offset-3" >
    <h5 class="card-title mt-2"><%= listing.title%></h5>
  <img 
  src="<%=listing.image.url%>" 
  class="card-img-top show-img"
   alt="...">
  <div class="card-body">
    <p class="card-text">
    <%= listing.description%>
    <br>
     &#8377;<%= listing.price.toLocaleString("en-IN")%>
     <br>
     <%= listing.location %>
     <br>

     <%= listing.country %>
    </p>
     </div>
  </div>
    <br/>
  
    
      <div class="btns">
    <a href="/listings/<%= listing._id %>/edit" class="btn btn-dark col-1 offset-2">Edit</a>
 
     <br /><br />
     <div class="col-1">
    <form method="POST" action="/listings/<%=listing._id%>?_method=DELETE">
      <button class="btn btn-dark offset-4">Delete </button>
    </form> 
    </div>
    </div>
    
 
  
 
    -->

    <% layout("/layouts/boilerplate") %>
<div class="row mt-3">
  <div class="col-8 offset-2">
    <h3>Listing Details</h3>
  </div>

  <!-- Listing Card -->
  <div class="card col-6 offset-3 mt-3 show-card">
    <h5 class="card-title mt-2 text-center"><%= listing.title %></h5>
    <img 
      src="<%= listing.image.url %>" 
      class="card-img-top show-img"
      alt="...">
    <div class="card-body">
      <p class="card-text">
        <%= listing.description %><br>
        &#8377;<%= listing.price.toLocaleString("en-IN") %><br>
        <%= listing.location %><br>
        <%= listing.country %>
      </p>
    </div>
  </div>

  <!-- Buttons Section -->
  <div class="row mt-4 mb-5">
    <div class="col-2 offset-3 d-grid">
      <a href="/listings/<%= listing._id %>/edit" class="btn btn-dark">Edit</a>
    </div>
    <div class="col-2 d-grid">
      <form method="POST" action="/listings/<%= listing._id %>?_method=DELETE">
        <button class="btn btn-dark del-btn">Delete</button>
      </form>
    </div>
  </div>
</div>
